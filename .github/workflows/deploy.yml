name: Deploy to Portainer

on:
  push:
    branches:
      - main

jobs:
  trigger-portainer:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Portainer Webhook
        run: |
          curl -X POST https://portainer.giovanc.xyz/api/stacks/webhooks/2635ebcc-cb78-4837-aa4e-2ef44f6a7698
